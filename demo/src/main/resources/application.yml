server:
 tomcat:
  max-swallow-size: -1


product:
 fruit:
  names: Apple, Lemon
  

spring:
 mvc:
  servlet:
   load-on-startup: 1
 datasource:
  #url: jdbc:h2:mem:mydb;INIT=CREATE SCHEMA IF NOT EXISTS mydb
  url: jdbc:h2:mem:testdb
  # File-based storage
  #url: jdbc:h2:file:/data/demo
  username: sa
  password:
  driverClassName: org.h2.Driver
 jpa:
  database-platform: org.hibernate.dialect.H2Dialect
  # Populates the data after the schema is generated by Hibernate
  #defer-datasource-initialization: true
 # Disable initialize the database
 #sql.init.mode: never
 #Hibernate will attempt to create the schema and tables every time the application start
  hibernate:
   ddl-auto: none
 # Enable H2 console
 h2:
  console:
   enabled: true
 # Handle file
 servlet:
  multipart:
   max-file-size: 10MB
   max-request-size: 20MB
 
path:
 file:
  download: D:/hoc hanh/FPT/Workspace/Spring_Boot_3/download/MarketAnalysisSeptember2021.pdf
  upload: D:/hoc hanh/FPT/Workspace/Spring_Boot_3/upload/
 